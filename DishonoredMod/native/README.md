# Нативный слой

DLL, которая грузится в процесс игры и раздаёт игровые события активному
режиму. Всё, что нельзя сделать конфигом — состояние режима, счёт, выбор
персонажа, свой UI, — живёт здесь.

## Состояние

Слой загружается в процесс игры — подтверждено окном и логом. Сигнатура
`ProcessEvent` снята с розничной сборки 1.0 и вписана в `native.ini`, хук
включён.

Адрес таблицы имён `GNames` статически не берётся: на диске глобалы нулевые,
таблица появляется только в рантайме. Поэтому плагин подбирает её раскладку
сам — см. `ue3detect.cpp` — и пишет найденное в лог готовыми строками для
конфига.

## Сборка

Игра 32-битная, поэтому и DLL обязана быть x86. CMake проверяет это и падает
с внятным сообщением, если конфигурация 64-битная.

**MSVC** (предпочтительно — игра собрана им):

```
cmake -A Win32 -B build
cmake --build build --config Release
```

**MinGW, в том числе кросс-сборкой из Linux:**

```
cmake -B build -DCMAKE_TOOLCHAIN_FILE=cmake/mingw-i686.cmake -DCMAKE_BUILD_TYPE=Release
cmake --build build
```

На выходе `dinput8.dll`.

## Тесты

```
cmake -B build-tests tests && cmake --build build-tests && ./build-tests/test_bytepattern
```

Проверяются разбор сигнатур и кодирование относительного перехода — то, что
роняет игру мгновенно и без внятного стека. Тесты не трогают `windows.h` и
гоняются на любой машине.

## Установка

Проще всего установщиком: положи `install.exe`, `dinput8.dll` и `native.ini` в
одну папку и запусти установщик. Он найдёт игру через реестр Steam и разложит
файлы сам.

Вручную — два файла в папку с экзешником:

```
...\Dishonored\Binaries\Win32\
    Dishonored.exe
    dinput8.dll      ← плагин
    native.ini       ← его настройки
```

**Сторонний загрузчик не нужен.** Плагин сам является прокси-библиотекой
`dinput8`: игра честно импортирует её, поэтому он подменяет её напрямую и
пробрасывает все пять экспортов в настоящую `dinput8` из системной папки.

Раньше здесь был файл `.asi` и Ultimate ASI Loader. От этой схемы отказались:
два посредника вместо одного давали вдвое больше способов не сработать — не то
имя прокси, не та разрядность загрузчика, не та папка. Если остались файлы от
неё (`DishonoredModKit.asi`, лишние `d3d9.dll` или `winmm.dll`) — удали, две
прокси-библиотеки перехватывают ввод дважды.

При запуске плагин показывает окно с подтверждением загрузки и путём к логу.
Отключается строкой `ShowLoadMessage=0`.

## Диагностика

`diagnose.exe` из `tools/diagnose` читает таблицу импорта экзешника и говорит,
какие имена прокси вообще применимы к этой сборке, лежат ли файлы на месте, нет
ли лишних прокси и пускает ли папка на запись.

Он написан после того, как несколько заходов ушло на перебор гипотез вместо
измерения: все причины незагрузки выглядят снаружи одинаково — ничего не
происходит.

## Как была найдена ProcessEvent

Записано на случай, если понадобится повторить для другой сборки.

В релизе строки вырезаны не все: UE3 хранит текст в широких символах, и там
уцелела диагностика скриптовой машины — **«Infinite script recursion (%i calls)
detected»**. Ссылается на неё ровно один участок кода, внутри
`ProcessInternal`; та узнаётся по диспетчеру опкодов `mov edx,[edx*4+GNatives]`.

Дальше — кто её вызывает. Прямых вызовов два: один берёт `UFunction*` третьим
аргументом и дёргает её нативный указатель, это `CallFunction`. Второй
принимает `(UFunction*, void*)` через thiscall — это и есть `ProcessEvent`.
Подтверждается тем, что адрес лежит внутри таблицы виртуальных функций
`UObject`, среди семнадцати подряд указателей на код.

Адреса в сборке 1.0 (sha256 `66443f3d...e17e`):

| Что | Адрес |
|---|---|
| `ProcessEvent` | `0x0046F8D0` |
| `ProcessInternal` | `0x0045FF10` |
| `CallFunction` | `0x00470230` |
| `GNatives` | `0x014068F0` |

Сигнатуре понадобилось 44 байта: на 33 она всё ещё цепляла две функции.
Замаскированы восемь байт — адрес обработчика SEH и адрес защитного cookie,
единственное, что меняется между сборками.

Пролог ровно 5 байт (`push ebp` + `mov ebp,esp` + `push -1`), граница перехода
попадает точно на конец инструкции.

Слой сам защищается от двух самых частых ошибок: если сигнатура не найдена
или найдена больше одного раза, хук не ставится и в лог идёт причина.
Неоднозначная сигнатура опаснее ненайденной — она молча цепляет не ту функцию.

## Устройство

| Файл | Что делает |
|---|---|
| `dllmain.cpp` | точка входа, чтение `native.ini`, установка хука |
| `bytepattern.h` | разбор сигнатур и кодирование перехода, без `windows.h` |
| `sigscan.cpp` | поиск сигнатуры в памяти главного модуля |
| `detour.cpp` | перехват функции через JMP с трамплином |
| `mode.cpp` | реестр режимов, встроенный режим-наблюдатель |
| `ue3.h` | соглашение вызова `ProcessEvent`, привязки к билду |
| `log.cpp` | лог в файл: папка из конфига, иначе запасные |
| `proxy_dinput8.cpp` | подмена `dinput8` и проброс её экспортов |
| `ue3names.cpp` | разрешение имён объектов через `GNames` |
| `ue3detect.cpp` | подбор раскладки таблицы имён в рантайме |

Работа в `DllMain` намеренно сведена к запуску потока: он вызывается под
блокировкой загрузчика, где нельзя ни грузить модули, ни ждать синхронизацию.
