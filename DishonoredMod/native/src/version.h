#pragma once

// Номер сборки нативного слоя.
//
// Существует ради одной конкретной ошибки, которая уже случалась дважды: файлы
// мода обновляются вручную, копирование может не пройти — игра держит DLL
// открытой, путь оказался не тот, — и тогда работает прежняя версия. Снаружи
// это выглядит как «исправление не помогло», и следующий круг уходит на поиск
// несуществующей поломки.
//
// Номер печатается первой строкой лога и первой строкой окна, поэтому по любому
// отчёту сразу видно, какая сборка на самом деле работает. Поднимается при
// каждой выдаче файлов наружу.
#define DMK_BUILD_NUMBER 7
