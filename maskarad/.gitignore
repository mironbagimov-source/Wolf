# Godot генерирует это при импорте проекта — в репозитории не нужно
.godot/
*.translation

# Текстуры, распакованные импортёром из FBX: движок достаёт их из модели сам
# при первом открытии проекта (проверено — удалил и вернулись). В репозитории
# им делать нечего, это лишние 100 МБ поверх самих моделей.
assets/characters/*.png
assets/characters/*.png.import
